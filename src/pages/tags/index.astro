---
import { url } from "@/utils/url-utils";

// 兼容重定向：将标签查询参数平滑导向课程中心的即时搜索
return Astro.redirect(
	Astro.url.searchParams.get("tag")
		? url(
				`/courses/?q=${encodeURIComponent(Astro.url.searchParams.get("tag") || "")}`,
			)
		: url("/courses/"),
);
---
